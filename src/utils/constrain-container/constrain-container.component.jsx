import './constrain-container.styles.scss'

const ConstrainContainer = ({children}) => {
    return(
        <div className="constrain-container">
                {children}
        </div>
    )
}

export default ConstrainContainer